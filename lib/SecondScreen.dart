import 'package:flutter/cupertino.dart';

class SecondScreen extends StatelessWidget {
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
          'It is the Second screen that will be load',
          style: TextStyle(fontSize: 32),
        ),
      ),
    );
  }
}
