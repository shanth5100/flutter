import 'package:flutter/cupertino.dart';

class FirstScreen extends StatelessWidget {
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
          'It is the first screen that will be load atht rge ',
          style: TextStyle(fontSize: 32),
        ),
      ),
    );
  }
}
